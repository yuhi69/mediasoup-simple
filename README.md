## 変更箇所
`/cert`配下の証明書を自身の証明書に置き換えてください。  
`index.js`内の`announcedIp`を実行するマシンのIPアドレスに変更してください。

## 実行
```
node index.js
```

ブラウザで以下にアクセスします。  
```
https://<サーバ名>:8443/video-room.html
```
固定画面で３人まで接続できます。
